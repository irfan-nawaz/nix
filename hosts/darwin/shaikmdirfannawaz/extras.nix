# Host-specific overrides for shaikmdirfannawaz. Empty by default --
# add system packages, sops secrets, or option overrides here when
# this host needs to diverge from hosts/common/darwin.nix.
_: { }
