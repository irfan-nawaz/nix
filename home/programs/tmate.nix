# tmate: tmux fork for instant ssh sharing. Default relay is
# tmate.io; uncomment and point at a self-hosted relay if you ever
# stand one up.
_: {
  # programs.tmate.extraConfig = ''
  #   set -g tmate-server-host    "tmate.PLACEHOLDER.example.com"
  #   set -g tmate-server-port    22
  #   set -g tmate-server-rsa-fingerprint    "PLACEHOLDER"
  #   set -g tmate-server-ed25519-fingerprint "PLACEHOLDER"
  # '';
}
