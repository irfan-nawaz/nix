# brave: Chromium-based browser. On macOS the GUI app stores prefs
# inside its own app bundle, so the HM module is effectively an install
# hook only -- the declarative profiles/extensions plumbing is Linux-
# only. Manage extensions via the in-app store.
_: { }
