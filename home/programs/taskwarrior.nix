# taskwarrior: CLI todo manager with sync (taskd / taskchampion). HM
# module owns ~/.taskrc; data lives at ~/.task by default.
#
# TODO: flip enable; add a sync server block if you want multi-device.
_: {
  # programs.taskwarrior = {
  #   enable = false;
  #   package = pkgs.taskwarrior3;
  #   config = {
  #     uda.priority.values  = "H,M,L,";
  #     urgency.user.tag.next.coefficient = 15.0;
  #     news.version = "3.0.0";
  #     # sync.server.origin     = "https://PLACEHOLDER.example.com:53589";
  #     # sync.server.client_id  = "PLACEHOLDER";
  #     # sync.encryption_secret = "PLACEHOLDER";
  #   };
  # };
}
