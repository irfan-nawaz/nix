# joplin-desktop: markdown notebook with optional sync (Joplin Cloud,
# Nextcloud, WebDAV, S3). HM module is enable-only; sync target is set
# in-app and credentials stay in macOS Keychain, not the nix store.
_: { }
