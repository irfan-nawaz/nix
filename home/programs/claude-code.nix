# claude-code: Anthropic's CLI. Auth + per-project memory live under
# ~/.claude/ and ~/.claude/projects/<repo>/. The HM module is thin --
# mostly an install hook. Project-scoped CLAUDE.md files in each repo
# do the heavy lifting; nothing meaningful to declare globally yet.
_: { }
