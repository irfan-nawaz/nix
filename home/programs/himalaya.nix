# himalaya: CLI mail client. Pairs nicely with mbsync for offline reads
# and msmtp for sends. HM module is per-account.
#
# TODO: add a himalaya block under accounts.email.accounts.<name>.
_: {
  # accounts.email.accounts.work.himalaya.enable = false;
  # programs.himalaya.enable = false;
}
