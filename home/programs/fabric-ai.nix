# fabric-ai (Daniel Miessler's `fabric`): pattern-based LLM prompt
# runner. Config lives at ~/.config/fabric/.env (API keys + default
# vendor). HM module is enable-only.
#
# TODO: run `fabric --setup` once, then move secrets to sops-nix and
# symlink them via xdg.configFile.
_: { }
