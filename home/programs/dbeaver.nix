# dbeaver: GUI universal DB client. HM module is enable-only; saved
# connections + drivers live under ~/Library/DBeaverData/ on macOS.
# Use cloud sync inside the app for cross-machine connection sharing.
_: { }
