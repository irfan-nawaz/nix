# ttyper: TUI typing trainer. HM module exposes only `enable`; the
# binary takes word lists + theme flags inline:
#   $ ttyper -w 50 -l english200
# Drop a wrapper script under ~/.local/share/scripts/ if you want a
# preset (script-directory will pick it up).
_: { }
