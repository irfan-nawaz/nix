# intelli-shell: bookmark + recall layer over shell history. Default
# bindings (Ctrl-Space etc.) are wired by the zsh integration that the
# HM module turns on automatically.
#
# TODO: add custom keybindings + theme via xdg.configFile if defaults
# clash with your atuin / hstr bindings.
_: {
  # xdg.configFile."intelli-shell/config.toml".text = ''
  #   [keybindings]
  #   search = "ctrl+space"
  # '';
}
