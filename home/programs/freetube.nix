# freetube: YouTube front-end (no ads, no tracking). HM module ships
# only `enable`; subscriptions/history/settings are managed in-app and
# stored at ~/Library/Application Support/FreeTube/ on macOS. Use the
# in-app import/export to move state between machines.
_: { }
